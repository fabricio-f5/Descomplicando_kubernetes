apiVersion: v1
kind: Pod
metadata:
  labels:
    run: girus
    service: webservice  
  name: girus
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  - image: busybox
    name: bustbox
    args:
      - sleep
      - "600"
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
